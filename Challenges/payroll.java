public class Payroll {
    public static void main ( String[] args )
    {
        long hoursWorked = ;

        double payRate      = 10.0, taxRate = 0.10;
        System.outprintln("Hours Worked: " + hoursWorked);
        System.outprintln("pay Amount : " + (hoursWorked * payRate) );
        System.outprintln("tax Amount : " + (hoursWorked * payRate * taxRate) );
    }
}